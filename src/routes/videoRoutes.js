import { Router } from 'express';
export function videoRoutes(controller) { const router = Router(); router.get('/', controller.search); router.get('/available-for-rental', controller.availableForRental); router.post('/', controller.create); router.post('/:id/copies', controller.addCopies); router.get('/:id/copies/available', controller.available); router.get('/:id/copies/removed', controller.removed); router.post('/copies/:copyId/removal', controller.removeCopy); return router; }